<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Kantin-Ku"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KantinKu"
        android:usesCleartextTraffic="true">

        <!-- Login Activity (LAUNCHER) -->
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (Home) -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Cart Activity -->
        <activity
            android:name=".ui.cart.CartActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <!-- Payment Activity -->
        <activity
            android:name=".ui.payment.PaymentActivity"
            android:exported="false"
            android:parentActivityName=".ui.cart.CartActivity" />

        <!-- Orders Activity (Tambahan jika ada) -->
        <activity
            android:name=".ui.orders.OrdersActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity" />

        <activity
            android:name=".ui.order.OrderStatusActivity"
            android:exported="false"
            android:parentActivityName=".ui.cart.CartActivity" />

    </application>

</manifest>